export enum AsyncStatus {
  SUCCEEDED = "Succeeded",
  FAILED = "Failed",
  CANCELED = "Canceled",
  RUNNING = "Running",
}

export enum ResourceStatus {
  HEALTHY = "Healthy",
  UNHEALTHY = "Unhealthy"
}
